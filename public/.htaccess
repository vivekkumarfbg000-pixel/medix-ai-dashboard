<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # 1. Route all Supabase Proxy requests to the PHP script
  RewriteRule ^supabase-proxy/(.*)$ supabase-proxy/index.php [QSA,L]

  # 2. Prevent directory listing
  Options -Indexes
</IfModule>
