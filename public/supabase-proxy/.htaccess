<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /supabase-proxy/
  # Route all sub-paths through index.php (the PHP proxy)
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
